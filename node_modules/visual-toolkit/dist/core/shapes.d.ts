/**
 * SHAPE HELPERS
 *
 * Organic shapes have asymmetric curves. Mechanical shapes have precise details.
 * This module provides helpers for both.
 */
/**
 * Generate organic border-radius values
 * Real creatures aren't perfectly round - asymmetry is key
 */
export declare function organicRadius(): string;
/**
 * Preset organic border-radius patterns
 */
export declare const organicRadii: {
    /** Slightly asymmetric blob */
    readonly blob: "60% 50% 45% 55%";
    /** Fish body shape */
    readonly fish: "60% 40% 40% 60%";
    /** Jellyfish bell */
    readonly bell: "50% 50% 40% 40%";
    /** Creature head */
    readonly head: "55% 45% 50% 50%";
    /** Flowing tail */
    readonly tail: "30% 70% 60% 40%";
};
/**
 * Draw panel lines on a surface (ROV style)
 *
 * @deprecated Use Scene-First architecture. Pattern A helpers being phased out in v3.0.
 * See: docs/SCENE_FIRST_ARCHITECTURE.md
 */
export declare function drawPanelLines(ctx: CanvasRenderingContext2D, x: number, y: number, width: number, height: number, linePositions?: number[], // normalized positions
color?: string): void;
/**
 * Draw grille/vent pattern (thruster style)
 *
 * @deprecated Use Scene-First architecture. Pattern A helpers being phased out in v3.0.
 * See: docs/SCENE_FIRST_ARCHITECTURE.md
 */
export declare function drawGrille(ctx: CanvasRenderingContext2D, x: number, y: number, width: number, height: number, lineCount?: number, color?: string): void;
/**
 * Draw a dashed tether/cable
 */
export declare function drawTether(ctx: CanvasRenderingContext2D, startX: number, startY: number, endX: number, endY: number, time: number, wobbleAmount?: number, color?: string): void;
/**
 * Draw a lens with glint
 */
export declare function drawLens(ctx: CanvasRenderingContext2D, x: number, y: number, outerRadius: number, innerRadius: number, outerColor?: string, innerColor?: string, glintColor?: string): void;
/**
 * Draw a light housing with optional glow
 */
export declare function drawLight(ctx: CanvasRenderingContext2D, x: number, y: number, radius: number, isOn: boolean, time: number): void;
/**
 * Draw creature eye that tracks a point
 */
export declare function drawTrackingEye(ctx: CanvasRenderingContext2D, eyeX: number, eyeY: number, eyeSize: number, targetX: number, targetY: number, illumination?: number): void;
/**
 * Draw tentacle segment path
 */
export declare function drawTentacle(ctx: CanvasRenderingContext2D, segments: Array<{
    x: number;
    y: number;
}>, thickness: number, color?: string): void;
