/**
 * Animation Loop Manager
 *
 * Handles requestAnimationFrame lifecycle with frame counting
 * and delta time calculation
 */
export declare class AnimationLoop {
    private onFrame;
    private animationId;
    private frameCount;
    private lastFrameTime;
    private isRunning;
    constructor(onFrame: (frameNum: number, deltaTime: number) => void);
    /**
     * Start animation loop
     */
    start(): void;
    /**
     * Stop animation loop
     */
    stop(): void;
    /**
     * Reset frame counter
     */
    reset(): void;
    /**
     * Get current frame number
     */
    getFrameNum(): number;
    /**
     * Check if animation is running
     */
    isRunning_(): boolean;
    private render;
}
