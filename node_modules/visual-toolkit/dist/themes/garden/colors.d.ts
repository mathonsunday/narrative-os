/**
 * Garden Theme Color Palettes
 *
 * Four distinct garden viewing experiences:
 * - Soft Garden: Warm, sunlit, peaceful ambient air
 * - Terrarium: Layered depth, specimen study perspective
 * - Garden Window: Muted, distant, through-glass barrier
 * - Overgrown: Rich, wild, nature reclaiming space
 */
/**
 * Soft Garden Window - Sunlit pollen and warm air
 * Aesthetic: Peaceful afternoon light with floating particles
 */
export declare const softGarden: {
    background: {
        light: string;
        mid: string;
        dark: string;
    };
    accents: {
        pollen: string;
        warmth: string;
        sunRay: string;
    };
    particles: {
        pollen: {
            bright: string;
            mid: string;
            dim: string;
        };
    };
};
/**
 * Terrarium View - Enclosed botanical ecosystem
 * Aesthetic: Looking into layered garden with depth
 */
export declare const terrarium: {
    background: {
        surface: string;
        mid: string;
        deep: string;
        shadow: string;
    };
    foliage: {
        new: string;
        mature: string;
        shadow: string;
        accent: string;
    };
    particles: {
        leaves: {
            light: string;
            mid: string;
            dark: string;
        };
    };
};
/**
 * Garden Through Glass - Muted, distant, observed
 * Aesthetic: Viewing garden through frosted/smudged glass barrier
 */
export declare const gardenWindow: {
    glass: {
        tint: string;
        edge: string;
        smudge: string;
    };
    garden: {
        muted: string;
        soft: string;
        distant: string;
    };
    particles: {
        dust: {
            bright: string;
            dim: string;
            veryDim: string;
        };
    };
};
/**
 * Overgrown Camera - Wild, invasive growth
 * Aesthetic: Nature reclaiming the camera/viewport
 */
export declare const overgrown: {
    background: {
        dense: string;
        vibrant: string;
        light: string;
    };
    growth: {
        vine: string;
        leaf: string;
        wild: string;
        shadow: string;
    };
    particles: {
        fragments: {
            bright: string;
            mid: string;
            dim: string;
        };
    };
};
/**
 * Helper function: Get color with optional opacity override
 * @example
 * const color = withAlpha('#ff0000', 0.5); // 'rgba(255, 0, 0, 0.5)'
 */
export declare function withAlpha(hexColor: string, alpha: number): string;
/**
 * Interpolate between two hex colors
 * @example
 * const mid = interpolateColor('#c8d5b9', '#4a6b47', 0.5); // Halfway blend
 */
export declare function interpolateColor(color1: string, color2: string, t: number): string;
