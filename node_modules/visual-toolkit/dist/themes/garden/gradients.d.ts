/**
 * Garden Theme Gradients
 *
 * Background gradient functions for each viewer window scene.
 * These establish the foundational atmosphere for each garden aesthetic.
 */
/**
 * Soft Garden Window - Warm, sunlit gradient
 * Creates peaceful afternoon light atmosphere
 */
export declare function softGardenBackground(ctx: CanvasRenderingContext2D, width: number, height: number): void;
/**
 * Terrarium View - Multi-layer depth gradient
 * Creates "looking into" perspective with layered greens
 */
export declare function terrariumBackground(ctx: CanvasRenderingContext2D, width: number, height: number): void;
/**
 * Garden Through Glass - Muted, distant gradient
 * Creates soft, obscured garden view (like looking through frosted glass)
 */
export declare function gardenWindowBackground(ctx: CanvasRenderingContext2D, width: number, height: number): void;
/**
 * Overgrown Camera - Rich, wild gradient
 * Creates dense, deep green background for invasive growth
 */
export declare function overgrownBackground(ctx: CanvasRenderingContext2D, width: number, height: number): void;
/**
 * Create depth layers helper
 * Generates semi-transparent rectangles for atmospheric perspective
 * @param ctx Canvas context
 * @param baseColor Base color to use for layers
 * @param width Canvas width
 * @param height Canvas height
 * @param layerCount Number of depth layers (default: 3)
 */
export declare function applyDepthLayers(ctx: CanvasRenderingContext2D, baseColor: string, width: number, height: number, layerCount?: number): void;
/**
 * Draw sun ray beams
 * Creates diagonal light beams for Soft Garden aesthetic
 */
export declare function drawSunRays(ctx: CanvasRenderingContext2D, width: number, height: number, rayCount?: number): void;
/**
 * Apply frosted glass texture effect
 * Subtle noise overlay for Garden Through Glass scene
 */
export declare function applyFrostedGlassEffect(ctx: CanvasRenderingContext2D, width: number, height: number, intensity?: number): void;
/**
 * Draw atmospheric depth fog
 * Creates sense of atmospheric perspective
 */
export declare function drawAtmosphericFog(ctx: CanvasRenderingContext2D, width: number, height: number, baseAlpha?: number): void;
