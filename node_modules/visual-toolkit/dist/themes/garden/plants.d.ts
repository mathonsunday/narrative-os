/**
 * Garden Theme Plant Elements
 *
 * Reusable leaf, petal, and vine drawing functions
 * Used to compose the layered scenes with organic plant shapes
 */
/**
 * Draw a simple leaf shape using Bezier curves
 * Used for background and layering in scenes
 */
export declare function drawSimpleLeaf(ctx: CanvasRenderingContext2D, x: number, y: number, size: number, color: string, rotation?: number): void;
/**
 * Draw a pointed/elongated leaf
 * For more dynamic variety in foliage
 */
export declare function drawPointedLeaf(ctx: CanvasRenderingContext2D, x: number, y: number, size: number, color: string, rotation?: number): void;
/**
 * Draw a cluster of overlapping leaves
 * Creates dense foliage appearance
 */
export declare function drawLeafCluster(ctx: CanvasRenderingContext2D, x: number, y: number, leafCount?: number, size?: number, color?: string, density?: number): void;
/**
 * Draw a petal shape
 * Used for scattered accent elements
 */
export declare function drawPetal(ctx: CanvasRenderingContext2D, x: number, y: number, width: number, height: number, color: string, rotation?: number): void;
/**
 * Draw a vine segment with natural wobble
 * Used for Overgrown scene
 */
export declare function drawVineSegment(ctx: CanvasRenderingContext2D, x1: number, y1: number, x2: number, y2: number, thickness: number, color: string, wobbleAmount?: number): void;
/**
 * Draw organic vine with branching
 * Creates natural-looking plant growth
 */
export declare function drawOrganicVine(ctx: CanvasRenderingContext2D, x: number, y: number, length: number, angle: number, thickness: number, color: string, depth?: number, maxDepth?: number): void;
/**
 * Draw a flower composed of petals
 * Optional accent element for scenes
 */
export declare function drawFlower(ctx: CanvasRenderingContext2D, x: number, y: number, petalCount?: number, petalSize?: number, color?: string, centerColor?: string): void;
/**
 * Draw multiple scattered flowers
 * Creates focal interest points
 */
export declare function drawFlowerCluster(ctx: CanvasRenderingContext2D, x: number, y: number, count?: number, spread?: number): void;
/**
 * Draw blurred background plant (for depth)
 * Used in Terrarium and Garden Window scenes
 */
export declare function drawBlurredPlant(ctx: CanvasRenderingContext2D, x: number, y: number, size: number, color: string, blur?: number): void;
/**
 * Draw plant silhouette (sharp foreground element)
 * Used for clear foreground layers
 */
export declare function drawPlantSilhouette(ctx: CanvasRenderingContext2D, x: number, y: number, height: number, color: string, leafCount?: number): void;
